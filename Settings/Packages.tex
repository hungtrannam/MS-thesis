\usepackage[utf8]{vietnam} % Gói lệnh này được sử dụng để hỗ trợ việc gõ tiếng Việt trong tài liệu LaTeX.
\usepackage[numbers,sort&compress]{natbib}

\usepackage[fontsize=13pt]{scrextend}

% Dùng để set margin
\usepackage[top=3cm,bottom=2cm,right=2cm,left=2cm,headheight=50pt]{geometry}		
\usepackage{enumerate}
\usepackage{enumitem}
\usepackage{graphicx}			% Dùng để chèn hình ảnh
\graphicspath{ {Figs/} }		% Folder chứa hình ảnh
\usepackage{subfiles}			% Chia thành các files nhỏ
\usepackage{emptypage}			% Không đánh số trang, header, footer với các trang trắng
\usepackage{tabularx}			% Dùng để chèn bảng
\usepackage{pdflscape}			% Một vài trang nằm ngang (landscape) thay vì dọc (portrait) (bảng, biểu đồ)
\usepackage{url}									% Dùng cho URL
\usepackage{longtable}								% Dùng cho các bảng dài, nhiều trang
\usepackage{makecell}								% Dùng để làm nổi bật header của bảng
\usepackage{float}								% Dùng để thiết đặt thuộc tính H (here) cho ảnh
\usepackage{amsmath,amssymb,bm,cool,mathtools,amsfonts, mathptmx}								% Dùng để chèn các công thức Toán
% Bảng màu dùng để dùng khi cần
\usepackage{xcolor}
\definecolor{CTUcyan}{rgb}{0, 0.69, 0.941}
\definecolor{CTUblue}{rgb}{0.122, 0.361, 0.667}

\usepackage{subcaption} % hèn nhiều hình ảnh trong cùng một hình với các chú thích riêng biệt.
\usepackage[format=plain,indent=\parindent,labelfont={sc,small},textfont={rm,small,stretch=1.15}]{caption}

\captionsetup[algorithm]{labelsep=colon}

\usepackage{booktabs}
\usepackage{multirow}
%\usepackage{algorithm2e}
\usepackage{algorithm} % viết các giải thuật trong tài liệu.
%\usepackage[noend]{algpseudocode}
\usepackage{algpseudocode}

\usepackage[hidelinks,unicode,pdfusetitle]{hyperref} % Chèn link
\hypersetup{
	colorlinks=true,
	citecolor=black,
	linkcolor=black,
	filecolor=black,      
	urlcolor=black,
} % Cài đặt colorlink = false khi in ấn

% Khoảng cách giữa các dòng
\linespread{1.2} 
% khoảng cách giữa các đoạn văn bản sẽ bằng với chiều cao của một dòng văn bản
\setlength{\parskip}{\baselineskip}%
% Load the parskip package with skip and indent options
\usepackage[skip=10pt plus1pt, indent=1cm]{parskip}

\usepackage[abbreviations,symbols,nogroupskip]{glossaries-extra}
\usepackage{glossary-mcols}
\renewcommand{\glsmcols}{2}
\setglossarystyle{mcolindex}

\usepackage[standard,thmmarks]{ntheorem}					
\usepackage[toc,page,title]{appendix} %phu luc


\usepackage{listings}
\lstset{frame=tb,
	language=Matlab,
	aboveskip=3mm,
	belowskip=3mm,
	showstringspaces=false,
	columns=flexible,
	basicstyle={\small\ttfamily\color{black!50}},
	numbers=none,
	numberstyle=\tiny\color{black!50},
	keywordstyle={\color{black}},
	commentstyle={\normalfont\color{black!40}},
	stringstyle={\it\ttfamily\color{black!40}},
	breaklines=true,
	breakatwhitespace=true,
	tabsize=3
}





\usepackage{tikz} % Vẽ hình trực tiếp lên LATEX


\usepackage{tkz-fct}
\usetikzlibrary{shapes.misc}

\usepackage{fancyhdr}
\usepackage{fancyvrb}
\usepackage{lineno}
\usepackage{titlesec}
\usepackage{indentfirst}
\usepackage{rotating}

